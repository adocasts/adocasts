<a href="{{ href }}" class="-m-3 p-3 flex items-center rounded-lg hover:bg-gray-50">
  @if (icon)
    <div class="flex-shrink-0 flex items-center justify-center h-10 w-10 rounded-md bg-brand-800 text-white">
      @!component(`components/svg/${icon}`, { className: 'w-6 h-6' })
    </div>
  @endif
  <div class="ml-4 text-base font-medium text-gray-950">
    {{ await $slots.main() }}
  </div>
</a>
