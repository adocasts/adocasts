@dropdown.base()
  @slot('triggerManual', props)
    @buttons.primary.inverse({ className: 'theme-selector', atClick: props.handler, square: true })
      <span class="theme-icon theme-icon-light text-slate-600 group-hover:text-slate-900">
        @!icons.sunFilled({ className: 'w-3 h-3 md:w-4 md:h-4'})
      </span>
      <span class="theme-icon theme-icon-dark text-slate-600 group-hover:text-slate-900">
        @!icons.moonFilled({ className: 'w-3 h-3 md:w-4 md:h-4'})
      </span>
      <span class="theme-icon theme-icon-system text-slate-600 group-hover:text-slate-900">
        @!icons.sunMoon({ className: 'w-3 h-3 md:w-4 md:h-4'})
      </span>
    @end
  @endslot

  @header.theme.item({ value: 'light' })
    <div class="flex items-center gap-x-3">
      @!icons.sunFilled()
      Light
    </div>
  @end

  @header.theme.item({ value: 'dark' })
    <div class="flex items-center gap-x-3">
      @!icons.moonFilled()
      Dark
    </div>
  @end

  @header.theme.item({ value: 'system' })
    <div class="flex items-center gap-x-3">
      @!icons.sunMoon()
      System
    </div>
  @end
@end