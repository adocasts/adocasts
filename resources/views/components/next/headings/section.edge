@set('slot', await $slots.main())
@set('slotTitle', $slots.title && await $slots.title())
@set('as', as ?? 'h2')

@if (slot || slotTitle || title || subtitle)
  <div class="{{ className }}">
    <{{ as }} class="text-3xl font-semibold mb-1 text-slate-900 dark:text-slate-100">
      @if (slotTitle)
        {{{ slotTitle }}}
      @else
        {{ title }}
      @endif
    </{{ as }}>
    @if (slot || subtitle)
      <p class="text-lg font-thin text-slate-600 dark:text-slate-400 max-w-prose {{ className?.includes('text-center') && 'mx-auto' }}">
        @if (slot)
          {{{ slot }}}
        @else
          {{ subtitle }}
        @endif
      </p>
    @endif
  </div>
@endif