@set('slot', await $slots.main())
@set('as', as ?? 'h2')

<div class="{{ className }}">
  <{{ as }} class="text-3xl font-semibold mb-1 text-slate-900 dark:text-slate-100">
    @if ($slots.title)
      {{{ await $slots.title() }}}
    @else
      {{ title }}
    @endif
  </{{ as }}>
  @if (slot || subtitle)
    <p class="text-lg font-thin text-slate-600 dark:text-slate-400 max-w-prose">
      @if (slot)
        {{{ slot }}}
      @else
        {{ subtitle }}
      @endif
    </p>
  @endif
</div>