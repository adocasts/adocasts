@set('action', action.includes('?') ? `${action}&_method=DELETE` : `${action}?_method=DELETE`)

<form method="POST" action="{{ action }}" {{{ attrs }}}>
  {{ csrfField() }}
  {{{ await $slots.main() }}}
</form>