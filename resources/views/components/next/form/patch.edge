@set('action', action.includes('?') ? `${action}&_method=PATCH` : `${action}?_method=PATCH`)

<form method="POST" action="{{ action }}" {{{ attrs }}}>
  {{ csrfField() }}
  {{{ await $slots.main() }}}
</form>