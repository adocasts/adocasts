@set('action', action.includes('?') ? `${action}&_method=PUT` : `${action}?_method=PUT`)

<form method="POST" action="{{ action }}" {{{ attrs }}}>
  {{ csrfField() }}
  {{{ await $slots.main() }}}
</form>