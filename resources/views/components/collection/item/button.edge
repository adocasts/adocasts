@button({ href: route('series.show', { slug: item.slug }), class: 'h-10 bg-white border-white !justify-start', classInner: '!gap-4', size: 'sm' })
  <div class="w-8">
    @!collection.icon({ item, class: 'h-full' })
  </div>
  <div class="flex-1">
    <h4 class="font-bold text-base">
      {{ item.name }}
    </h4>
  </div>
@end