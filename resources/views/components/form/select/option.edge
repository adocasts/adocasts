@let(selected = $context.selected === $props.value ? { selected: 'selected' } : {})

<option {{ $props.except(['text', 'value']).merge(selected).toAttrs() }} value="{{ value }}">
  @if (text)
    {{ text }}
  @else
    {{{ await $slots.main() }}}
  @endif
</option>
