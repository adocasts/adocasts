@set('color', color ?? 'dark')

<div class="py-3 flex flex-col mb-9">
  <h1 class="text-3xl font-extrabold {{ color === 'dark' ? "text-gray-950 dark:text-gray-200" : "text-gray-100" }} tracking-tight mb-2">
    {{{ await $slots.main() }}}
  </h1>
  @if ($slots.description)
    <p class="max-w-prose text-lg text-gray-700">
      {{{ await $slots.description() }}}
    </p>
  @endif
</div>
