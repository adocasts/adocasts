@layout()
  @discussion.shell({ feed: 'none', topics })

    <div>
      <a href="{{ route('discussions.index') }}" class="btn btn-soft">
        @svg('solar:double-alt-arrow-left-line-duotone')
        Return to Forum
      </a>
    </div>

    @!discussion.card({ discussion, show: true })
    @!comment.list({ comments: discussion.comments })

    @dump(discussion)
  @end
@end
